# GOT Character manager

To install the dependencies:

	pnpm install

To run this project in dev mode:

	pnpm dev

To run this project in production:

	pnpm build
	pnpm preview

To run the linter:

	pnpm lint

